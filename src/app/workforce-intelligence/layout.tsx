import Container from "@/components/container";

export default function WorkforceIntelligenceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <Container>
      {children}
    </Container>
  );
} 